'use client';

import React, { FC } from "react";

const HomePage: FC = () => (
  <div>
    {/* <Header />
    <SiteProvider>
      <Dashboard />
    </SiteProvider> */}
  </div>
);

// export default withAuth(HomePage);
export default HomePage;